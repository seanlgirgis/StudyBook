# Database Design Lab

This compact PostgreSQL lab turns the course concepts into one coherent design exercise.

Run the scripts in numeric order from `lab/sql`. The lab is intentionally small: it demonstrates operational modeling, dimensional modeling, views, access control, and range partitioning without becoming a separate application.

Status: **DEVELOPING** until executed locally and observed results are recorded.
