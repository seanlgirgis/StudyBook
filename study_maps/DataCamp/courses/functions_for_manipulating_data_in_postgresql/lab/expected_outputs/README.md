# Expected Output Notes

The seed data contains:

- 4 customers;
- 5 films with text-array special features;
- 6 rentals across several months;
- one unreturned rental designed to become overdue relative to its rental timestamp.

Exact `CURRENT_TIMESTAMP` comparisons depend on the day the lab is run. Static exercises use seeded timestamps for reproducibility.
