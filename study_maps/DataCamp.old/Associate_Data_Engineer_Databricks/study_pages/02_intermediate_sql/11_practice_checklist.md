# Course 2: Intermediate SQL - Practice Checklist

Status: partial checkpoint

- [ ] Write three queries demonstrating `COUNT(*)`, `COUNT(column)`, and `COUNT(DISTINCT column)`.
- [ ] Rewrite one bad `DISTINCT COUNT(...)` example into correct form.
- [ ] Build one `BETWEEN` filter and verify inclusiveness with boundary values.
- [ ] Convert one OR-heavy filter into an `IN (...)` filter.
- [ ] Write five `LIKE` pattern examples using `%` and `_`.
- [ ] Run one `IS NULL` and one `IS NOT NULL` completeness query.
- [ ] Compute one filtered aggregate (`AVG`, `SUM`, or `COUNT`) using `WHERE`.
- [ ] Use `ROUND(..., -2)` and explain why it is useful.
- [ ] Demonstrate integer division vs decimal division.
- [ ] Fix one alias-in-WHERE issue by expanding expression in WHERE.
\n\n## Completion Delta\n- Added ORDER BY, GROUP BY, HAVING, percentage arithmetic, span/decade, unit conversion, and final completion classification coverage.
