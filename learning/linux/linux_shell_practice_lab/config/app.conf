# Application configuration
app_name=interview-practice
environment=dev
debug=false

# database
db_host=localhost
db_port=5432
db_name=practice

# timeouts
connect_timeout=30
read_timeout=60
job_timeout=300

# feature flags
enable_cache=true
enable_retries=true
