# 03g_retrieval_decision

## POC Name
`03g_retrieval_decision`

## Purpose
Build a deterministic retrieval-decision layer on top of `03f_hybrid_retrieval` results.

This POC does not perform retrieval itself. It inspects retrieval evidence and assigns a quality/confidence decision label for downstream handling.

## Why This Exists
`03f` returns ranked candidates and scores, but it does not decide whether those results are strong, ambiguous, weak, or unusable.

`03g` exists to make that decision explicit, testable, and reusable before any answer generation or business response behavior is introduced.

## Retrieval Ladder Position
- `03f_hybrid_retrieval`: produces ranked hybrid candidates
- `03g_retrieval_decision`: decides retrieval confidence/quality class
- future steps: use decision output for clarification UX, fallback behavior, and evaluation

## Expected Input
- hybrid retrieval result payload from `pocs/03f_hybrid_retrieval/outputs/sample_hybrid_search_results.json` (or compatible structure)
- deterministic decision configuration (thresholds, gap rules, close-candidate rules)

## Expected Output
A structured retrieval-decision object with:
- decision label (`decision_label`)
- confidence band or score
- reason codes
- key evidence values (top score, score gap, close-candidate count, source diversity)
- recommended downstream route hint (`recommended_route`) for later POCs

## Expected Decision Labels
- `strong_match`
- `ambiguous_match`
- `weak_match`
- `no_match`
- `needs_clarification`

## Recommended Downstream Routes (Hints Only)
- `answer_candidate_path`
- `clarification_path`
- `fallback_path`
- `no_answer_path`

Important:
- `decision_label` is the retrieval-quality classification result.
- `recommended_route` is only a downstream routing hint and does not change the classification.

## Planned Rule Precedence (Deterministic)
1. `no_match`: no candidates or score below no-match floor.
2. `strong_match`: strong score with clear dominance.
3. `needs_clarification`: query/evidence suggests ambiguity requiring clarification, even if ambiguity or weakness also applies.
4. `ambiguous_match`: close competing candidates but query is specific enough to remain ambiguous (not clarification-forced).
5. `weak_match`: low but nonzero evidence without stronger precedence conditions.

Precedence is intentional so downstream behavior is predictable.

## Threshold Tuning Note
Any threshold values documented in this POC are initial placeholders only.
They should be tuned later with retrieval-evaluation fixtures and should not be treated as permanent truth.

## What 03g Must Not Do
- generate customer answers
- call an LLM
- decide final business response
- ask live clarification questions
- rebuild `03d`, `03e`, or `03f` artifacts
- duplicate prior retrieval logic
- move anything into `integrated/servicecall-ai`

## Deliverables In This Step
Implemented deliverables:
- `src/schemas.py`
- `src/retrieval_decision.py`
- `src/run_retrieval_decision.py`
- `config/decision_config.json`
- `tests/test_retrieval_decision.py`
- `outputs/sample_retrieval_decisions.json` (generated by runner)

## Commands
```powershell
# run 03g unit tests
pytest -v .\pocs\03g_retrieval_decision\tests

# generate sample retrieval-decision artifact from 03f output
python .\pocs\03g_retrieval_decision\src\run_retrieval_decision.py
```
