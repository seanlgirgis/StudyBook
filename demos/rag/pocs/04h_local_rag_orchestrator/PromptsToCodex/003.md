You are working in:

D:\Workarea\StudyBook\demos\rag

Task: Fix the 04h local orchestrator draft-answer cutoff issue.

Scope:
pocs/04h_local_rag_orchestrator only.

Do not redesign the POC.
Do not add Grok/OpenAI.
Do not add Docker.
Do not modify 04g or 04g-quantized.
Do not change the API contract unless absolutely necessary.

Problem observed:

The 04h smoke test works, but the generated draft answer can end mid-sentence or mid-word, for example:

"Finally, reinstall the P-tr"

This is not an orchestration failure. It is a response cleanup issue caused by local 8-bit LLM generation stopping before a complete final sentence.

Goal:

Improve draft_answer quality so returned answers do not end mid-word, mid-sentence, or with an obvious unfinished fragment.

Implement the smallest safe fix.

Preferred implementation:

1. In:

pocs/04h_local_rag_orchestrator/src/service.py

Add a helper function:

trim_to_complete_sentence(text: str) -> str

Behavior:
- strip whitespace
- if text is empty, return text
- find the last complete sentence-ending punctuation:
  ".", "!", "?"
- return text up to that last punctuation
- if no sentence-ending punctuation exists, return the original stripped text
- avoid returning an empty string if the only punctuation is too early
- preserve normal complete answers

Example behavior:
Input:
"Call us for help. We can inspect the leak. Finally, reinstall the P-tr"
Output:
"Call us for help. We can inspect the leak."

Input:
"An AC repair service fixes cooling problems."
Output:
"An AC repair service fixes cooling problems."

Input:
"Plumbing leak repair"
Output:
"Plumbing leak repair"

2. Apply this helper only to the final local LLM draft answer before returning response JSON.

For example:

raw_answer = call_local_llm(...)
draft_answer = trim_to_complete_sentence(raw_answer)

3. Strengthen the grounded prompt slightly, but keep it simple.

Where 04h assembles the prompt for the local LLM, add instructions like:

"Answer in 2 complete sentences."
"Do not continue into a new question, heading, checklist, or article."
"Stop after the answer."

Do not make the prompt huge.

4. Add or update tests.

Add tests for trim_to_complete_sentence:
- trims unfinished final fragment
- preserves complete answer
- preserves text with no punctuation

If current service tests are the best place, update:
pocs/04h_local_rag_orchestrator/tests/test_service.py

5. Re-run validation:

cd D:\Workarea\StudyBook\demos\rag
python -m pytest -q pocs/04h_local_rag_orchestrator/tests

Then with 8-bit LLM container running on port 8002:

cd D:\Workarea\StudyBook\demos\rag\pocs\04h_local_rag_orchestrator
python .\smoke_test_04h.py

6. Update outputs if smoke test is rerun:

pocs/04h_local_rag_orchestrator/outputs/SMOKE_TEST_RESULT.md

7. If tracking files already mention 04h status and it is easy to update safely, add a short note:
- Improved 04h draft answer cleanup to avoid mid-sentence/mid-word endings.
- Tests pass.
- Smoke output refreshed.

Return:

A) Changed files only
B) Summary of exact change
C) Validation commands and results
D) Any remaining limitation