You are working in:

D:\Workarea\StudyBook\demos\rag

Task: Extend 04h local RAG orchestrator with multi-sentence / multi-intent detection.

Scope:
pocs/04h_local_rag_orchestrator only.

Do not modify:
- pocs/04f
- pocs/04g
- pocs/04g-quantized
- integrated/servicecall-ai

Do not add Docker.
Do not add vector DB.
Do not add persistent sessions yet.
Do not make local 8-bit a final answer model.
Do not call Grok for multi-intent clarification cases.

Current architecture must remain:

- local 8-bit = intent clarification/classification engine only
- deterministic code = policy controller, retrieval, retry/escalation
- Grok/final provider = final customer answer only
- no local 8-bit final answer fallback

Current 04h already supports:
- supported
- clarification_needed
- unsupported
- human_escalation_required
- max clarification attempts = 3
- unsupported services like car AC and carpet cleaning
- final_provider_unavailable when Grok key is missing
- 15 tests passing

New goal:

Handle customer messages that contain multiple service intents in one message.

Examples:
1. "My AC is not cooling and there is water under my sink."
2. "My water heater pilot light keeps going out and my kitchen drain is clogged."
3. "My car AC is broken and my home AC is not cooling."
4. "I need maintenance, and also my refrigerator stopped cooling."

Business rule:

If multiple intents are detected, do not retrieve a final answer for all of them yet.
Do not call Grok.
Ask the customer which issue to handle first.

New classification value:

multi_intent

Add/extend output fields:

{
  "classification": "multi_intent",
  "intents": [
    {
      "classification": "supported|unsupported|clarification_needed",
      "cleaned_intent": "string",
      "service_type": "AC|heating|plumbing|water_heater|appliance|maintenance|emergency|unknown",
      "matched_capability": "string",
      "symptoms": ["string"],
      "unsupported_reason": "string",
      "confidence": 0.0
    }
  ],
  "clarification_needed": true,
  "clarifying_questions": [
    "I can help with more than one issue. Which should we handle first: AC not cooling or plumbing leak under the sink?"
  ],
  "retrieved_sections": [],
  "final_answer": "",
  "final_provider_used": "none",
  "status": "clarification_needed"
}

Rules:

1. If one message contains two or more supported service intents:
   - classification = multi_intent
   - status = clarification_needed
   - populate intents
   - ask which issue to handle first
   - no retrieval
   - no Grok

2. If one message contains supported + unsupported:
   - classification = multi_intent
   - include both intents
   - clarifying question should acknowledge boundary:
     "We do not service vehicle AC, but we can help with home AC. Do you want to continue with the home AC issue?"
   - no Grok

3. If one message is just one clear supported intent:
   - preserve current behavior

4. If one message is just unsupported:
   - preserve current behavior

5. If one message is vague:
   - preserve current clarification behavior

Implementation guidance:

1. Update local 8-bit intent prompt in service.py:
   - include instruction to detect multiple distinct service requests
   - include "classification": "multi_intent"
   - require an "intents" array if multi_intent
   - still Return JSON only
   - still Do not answer customer
   - still Do not give advice

2. Add deterministic fallback multi-intent detection:
   - split on sentence boundaries and conjunctions like "and also", "also", "plus"
   - detect service signals per segment
   - if 2+ distinct capabilities are found, return multi_intent
   - keep it simple and deterministic

3. Update answer_query:
   - if classification == multi_intent:
     - skip retrieval
     - skip Grok
     - return status clarification_needed
     - include intents
     - include clarifying question
     - include final_provider_used none

4. Update app.py response model/contract if needed:
   - preserve backward compatibility
   - include intents field as optional/list

5. Update interactive_hybrid_test.py:
   - print intents if present
   - print classification multi_intent clearly
   - print clarifying question

6. Update docs:
   - README.md
   - docs/DESIGN.md
   - docs/CONTRACT.md
   - docs/TEST_PLAN.md

Document:
- 04h Phase 2 supports multi-intent detection.
- Multi-intent is intentionally clarification-first.
- 04h does not answer multiple issues in one response yet.
- Customer must choose priority issue first.
- This is safer for service intake and future scheduling.

7. Update tests:

Add tests for:
- AC + plumbing leak => multi_intent
- water heater pilot + clogged drain => multi_intent
- car AC + home AC => multi_intent with unsupported + supported intent
- normal single AC query still supported
- normal car AC query still unsupported
- multi_intent does not call Grok
- multi_intent returns no retrieved_sections and no final_answer

Use monkeypatching.
No tests should require real Grok.
No tests should require live 8-bit container.

Validation:

Run:

cd D:\Workarea\StudyBook\demos\rag
python -m pytest -q pocs/04h_local_rag_orchestrator/tests

Then manually run:

cd D:\Workarea\StudyBook\demos\rag\pocs\04h_local_rag_orchestrator
python .\interactive_hybrid_test.py

Manual test inputs:

1.
My AC is not cooling and there is water under my sink.

Expected:
classification multi_intent
status clarification_needed
intents include AC repair and plumbing leak repair
clarifying question asks which issue to handle first
no retrieved sections
no final answer

2.
My car AC is broken and my home AC is not cooling.

Expected:
classification multi_intent
one unsupported vehicle AC intent
one supported home AC repair intent
clarifying question acknowledges unsupported vehicle AC and asks whether to continue with home AC

3.
My water heater pilot light keeps going out and my kitchen drain is clogged.

Expected:
classification multi_intent
intents include water heater pilot light and clogged drain
status clarification_needed

Return:

A) Changed files only
B) Summary of multi-intent behavior
C) Validation results
D) Example interactive outputs
E) Remaining limitations