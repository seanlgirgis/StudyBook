You are working in:

D:\Workarea\StudyBook\demos\rag

Task: Add an interactive hybrid test script to existing POC:

pocs/04h_local_rag_orchestrator

Purpose:

Create a local terminal script that proves the next hybrid flow before we containerize the orchestrator.

Architecture goal:

User messy request
  -> 04h orchestrator logic
  -> local 8-bit LLM for intent cleanup when available
  -> deterministic KB retrieval
  -> Grok-3 or configured Grok model for final polished answer when API key exists
  -> fallback to local 8-bit draft answer if Grok is unavailable
  -> print structured result
  -> append logs to outputs/hybrid_ask_logs.jsonl

Important:
Do not Dockerize 04h yet.
Do not modify 04g or 04g-quantized.
Do not move anything into integrated/servicecall-ai.
Do not redesign the existing 04h API.
Do not break current tests or smoke test.

Current validated dependencies:

1. 8-bit local LLM container:
- container: llm_7b_8bit_run
- endpoint: http://localhost:8002/infer
- health: http://localhost:8002/health

2. 04h app already works:
- uvicorn src.app:app --host 127.0.0.1 --port 8010
- smoke_test_04h.py passed
- cutoff cleanup passed

New file to create:

pocs/04h_local_rag_orchestrator/interactive_hybrid_test.py

Optional supporting file if needed:

pocs/04h_local_rag_orchestrator/src/grok_gateway.py

Implementation requirements:

1. interactive_hybrid_test.py

Behavior:
- Terminal loop:
  - print title:
    "=== 04h Hybrid Interactive RAG Tester ==="
  - type "exit" to quit
  - prompt:
    "Enter customer request: "
- For each query:
  - call service.answer_query(original_query)
    This returns the existing structured 04h result with:
      original_query
      cleaned_intent
      service_type
      symptoms
      urgency
      retrieved_sections
      draft_answer
      provider_used
      status
  - Then attempt final answer generation using Grok if available.
  - Print:
    - Original Query
    - Cleaned Intent
    - Service Type
    - Urgency
    - Retrieved Sections titles/ids/scores
    - Draft Answer from local 8-bit
    - Final Answer
    - Final Provider Used
    - Status

2. Grok final answer behavior

Use environment variables:
- XAI_API_KEY or GROK_API_KEY
- GROK_MODEL default: grok-3
- GROK_BASE_URL default: https://api.x.ai/v1

If API key exists:
- Call Grok chat completions API.
- Use retrieved sections as grounding context.
- Prompt Grok to produce a concise, customer-facing answer.
- It must not invent services not found in retrieved sections.
- It should ask a clarification if retrieved_sections is empty.
- It should be friendly and practical.

If API key does not exist or call fails:
- fallback to the local 8-bit draft_answer from service.answer_query
- final_provider_used should be "local_8bit_fallback"
- include a clear note in the printed output but do not crash

3. Suggested Grok prompt

System:
"You are a customer-facing assistant for North Texas Comfort & Home Services. Answer using only the provided knowledge base sections. Be concise, practical, and friendly. If the context is insufficient, ask one clarifying question."

User prompt should include:
- original_query
- cleaned_intent
- service_type
- urgency
- retrieved_sections as id/title/text
- local draft_answer as optional draft
- instruction:
  "Return only the final customer-facing answer."

4. Logging

Append one JSON object per interaction to:

pocs/04h_local_rag_orchestrator/outputs/hybrid_ask_logs.jsonl

Each log record:
{
  "timestamp_utc": "...",
  "original_query": "...",
  "cleaned_intent": "...",
  "service_type": "...",
  "urgency": "...",
  "retrieved_section_ids": ["..."],
  "local_draft_answer": "...",
  "final_answer": "...",
  "final_provider_used": "grok-3|local_8bit_fallback|error",
  "status": "answered|no_context|llm_unavailable|error"
}

5. Keep imports simple.

Do not add heavy dependencies.
Use Python standard library urllib or http.client for HTTP calls unless project already uses requests.
If requests is already available and used, okay to use it.

6. Tests

Add or update lightweight tests only if simple:
- test Grok prompt assembly if helper function exists
- test fallback behavior when no API key
- do not require real Grok call
- do not require local 8-bit container in unit tests

Do not overbuild this.

7. Documentation update

Update:
pocs/04h_local_rag_orchestrator/README.md
pocs/04h_local_rag_orchestrator/docs/DESIGN.md
pocs/04h_local_rag_orchestrator/docs/TEST_PLAN.md

Add short section:
"Interactive Hybrid Tester"

Document:
- local 8-bit LLM remains intake/draft helper
- Grok-3 or configured Grok model is optional final answer provider
- fallback behavior
- command to run

Command:
cd D:\Workarea\StudyBook\demos\rag\pocs\04h_local_rag_orchestrator
python .\interactive_hybrid_test.py

8. Validation

Run:

cd D:\Workarea\StudyBook\demos\rag
python -m pytest -q pocs/04h_local_rag_orchestrator/tests

Then manually run, with llm_7b_8bit_run running:

cd D:\Workarea\StudyBook\demos\rag\pocs\04h_local_rag_orchestrator
python .\interactive_hybrid_test.py

Try these inputs:
1. Hi, there is water under my sink and I think a pipe is leaking.
2. My AC is running but the house is not cooling.
3. My water heater pilot light keeps going out.
4. exit

Return:

A) Changed files only
B) What was added
C) How Grok fallback works
D) Validation results
E) Any remaining limitations