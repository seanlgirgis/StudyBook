# Decision Log

- Created repository control files so behavior is defined by local instructions instead of chat memory.
- Standardized memory lookup behavior to return stored values directly with source citation.
