# Command Allowlist

Prefer these commands for repository work:

- `rg` for text search
- `rg --files` for file listing
- `Get-Content -Raw` for file reads
- `tree /F` for tree output

Do not trust memory over repository content.
Always verify stored answers by reading the relevant file.
