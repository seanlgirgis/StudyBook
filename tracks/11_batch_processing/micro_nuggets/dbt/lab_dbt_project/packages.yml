# packages.yml — dbt package dependencies
# Kept empty for this lab to avoid external network requirements.
# To add packages, uncomment and run `dbt deps`:
#
# packages:
#   - package: dbt-labs/dbt_utils
#     version: [">=1.0.0", "<2.0.0"]
#
#   - package: elementary-data/elementary
#     version: [">=0.10.0"]

packages: []
