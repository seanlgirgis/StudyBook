# .env
# Database Mastery Stack — credentials and ports
# Location: D:\Workspace\Basics\Databases\_setup\.env
# NEVER commit this file — already in .gitignore
#
# Change passwords before first run.
# After changing, run: docker compose down && docker compose up -d

# ── PostgreSQL ──────────────────────────────────────────────────────
POSTGRES_USER=de_admin
POSTGRES_PASSWORD=DeAdmin2026!
POSTGRES_DB=de_telemetry
POSTGRES_PORT=5432

# ── Redis ───────────────────────────────────────────────────────────
REDIS_PASSWORD=DeRedis2026!
REDIS_PORT=6380

# ── Cassandra ───────────────────────────────────────────────────────
# Note: Cassandra auth is set after first boot via cqlsh
# Port only needed here for documentation
CASSANDRA_PORT=9042

# ── Neo4j ────────────────────────────────────────────────────────────
NEO4J_USER=neo4j
NEO4J_PASSWORD=DeNeo4j2026!
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687

# ── InfluxDB ─────────────────────────────────────────────────────────
INFLUXDB_USER=de_admin
INFLUXDB_PASSWORD=DeInflux2026!
INFLUXDB_ORG=de_org
INFLUXDB_BUCKET=telemetry
INFLUXDB_TOKEN=de-influxdb-super-secret-token-2026
INFLUXDB_PORT=8086

# ── Elasticsearch ────────────────────────────────────────────────────
ELASTIC_PASSWORD=DeElastic2026!
ELASTIC_PORT=9200

# ── Kibana ────────────────────────────────────────────────────────────
# kibana_system password must match what you set in ES after first boot
# See README.md Step 4 for the one-time setup command
KIBANA_PASSWORD=DeKibana2026!
KIBANA_PORT=5601

# ── Splunk ────────────────────────────────────────────────────────────
SPLUNK_PASSWORD=DeSplunk2026!
SPLUNK_PORT_WEB=8000
SPLUNK_PORT_HEC=8088
SPLUNK_PORT_MGT=8089

# ── MongoDB Atlas ─────────────────────────────────────────────────────────────
MONGO_URI=mongodb+srv://de_admin:DeAtlas2026!@de-learning.zur1dze.mongodb.net/?appName=de-learning
MONGO_DB=de_telemetry
