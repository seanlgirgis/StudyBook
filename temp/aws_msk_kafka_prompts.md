# AWS MSK / Kafka — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: AWS MSK and Apache Kafka
Slug: aws-msk-kafka
Extra coverage required: Kafka core concepts — topics, partitions, consumer groups, offsets, brokers,
MSK Serverless vs MSK Provisioned — when to choose each and cost tradeoffs,
MSK vs self-managed Kafka on EC2 — operational overhead vs control,
producer configuration tuning — acks, batching, compression, retries,
consumer configuration tuning — fetch size, poll interval, commit strategy,
consumer group rebalancing and how to minimize disruption,
exactly-once semantics — what it means and when you actually need it,
Glue Schema Registry with MSK for schema evolution and enforcement,
MSK Connect for managed Kafka connectors (S3 sink, JDBC source),
monitoring — CloudWatch consumer lag metrics, broker metrics, alerting thresholds,
MSK in data engineering — real-time ingestion to S3 via Firehose, Spark Streaming, Flink,
Kafka vs Kinesis — when to choose each for a data platform,
cost model and common bill surprises at scale.
```

---

## Project 2 — HTML Page

Run after `final_aws-msk-kafka.mp3` is live on R2.

```
Topic: AWS MSK and Apache Kafka
Slug: aws-msk-kafka
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_aws-msk-kafka.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
