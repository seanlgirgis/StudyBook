# CI/CD (GitHub Actions + Docker + ECS) — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: CI/CD with GitHub Actions, Docker, and ECS
Slug: cicd-github-ecs
Extra coverage required: GitHub Actions workflow structure — triggers, jobs, steps, runners,
Docker multi-stage builds for lean production images,
ECR as the image registry — push, tag, lifecycle policies,
ECS deployment strategies — rolling update vs blue/green with CodeDeploy,
OIDC authentication from GitHub Actions to AWS — no stored long-term credentials,
environment promotion — dev to staging to prod with approval gates,
secrets management in pipelines — GitHub Secrets, AWS Secrets Manager, Parameter Store,
caching Docker layers and dependencies for faster builds,
running tests in the pipeline — unit, integration, smoke tests,
infrastructure as code in CI/CD — Terraform or CloudFormation in the pipeline,
rollback strategies — what happens when a deployment fails,
data engineering CI/CD patterns — deploying Glue jobs, Lambda, Airflow DAGs,
common mistakes that cause silent deployment failures.
```

---

## Project 2 — HTML Page

Run after `final_cicd-github-ecs.mp3` is live on R2.

```
Topic: CI/CD with GitHub Actions, Docker, and ECS
Slug: cicd-github-ecs
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_cicd-github-ecs.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
