# AWS Bedrock — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: AWS Bedrock
Slug: aws-bedrock
Extra coverage required: foundation model families available on Bedrock
(Claude, Titan, Llama, Mistral, Cohere) and when to choose each,
Knowledge Bases for managed RAG — S3 as document source, vector store options,
chunking strategy and embedding model selection,
Agents for tool-use orchestration — action groups, Lambda integration, session memory,
Guardrails for content filtering and hallucination control,
embeddings with Titan Embed for semantic search over data lakes,
inference profiles and cost model (per input/output token),
data engineering use cases — LLM-powered ETL enrichment, semantic search,
pipeline anomaly explanation, natural language to SQL,
security — VPC endpoints for private access, IAM policies, encryption at rest,
Bedrock vs OpenAI API — when to choose each in an AWS-native stack,
latency and throughput limits for production workloads.
```

---

## Project 2 — HTML Page

Run after `final_aws-bedrock.mp3` is live on R2.

```
Topic: AWS Bedrock
Slug: aws-bedrock
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_aws-bedrock.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
