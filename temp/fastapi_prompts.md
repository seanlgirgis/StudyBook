# FastAPI — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: FastAPI
Slug: fastapi
Extra coverage required: async vs sync endpoints and when each matters for data workloads,
Pydantic models for request/response validation and why they matter at scale,
dependency injection pattern for shared resources (DB connections, auth, config),
background tasks for fire-and-forget pipeline triggers,
middleware and CORS configuration,
OAuth2 and JWT authentication patterns,
OpenAPI/Swagger auto-generation and why it matters for data APIs,
deploying FastAPI on ECS Fargate and AWS Lambda (Mangum adapter),
FastAPI as a data API layer — serving ML model predictions, query results, pipeline triggers,
connection pooling for database-backed endpoints,
testing with pytest and TestClient,
performance considerations — uvicorn workers, gunicorn, concurrency limits,
when FastAPI is the right choice vs alternatives.
```

---

## Project 2 — HTML Page

Run after `final_fastapi.mp3` is live on R2.

```
Topic: FastAPI
Slug: fastapi
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_fastapi.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
