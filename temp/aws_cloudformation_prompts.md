# AWS CloudFormation — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: AWS CloudFormation
Slug: aws-cloudformation
Extra coverage required: template structure — Resources, Parameters, Outputs, Conditions, Mappings,
stack vs nested stacks vs stack sets for multi-account deployments,
change sets — preview before you apply, safe production deployments,
rollback behavior — what triggers it and how to override it,
stack policies to prevent accidental resource deletion,
drift detection — find what changed outside CloudFormation,
custom resources with Lambda for resources CloudFormation does not support natively,
CloudFormation vs Terraform — strengths, weaknesses, when to choose each,
provisioning data engineering infrastructure — S3, Glue, EMR, Redshift, RDS, ECS, IAM roles,
StackSets for deploying the same stack across multiple accounts and regions,
AWS CDK as a higher-level abstraction that compiles to CloudFormation,
common mistakes — circular dependencies, deletion policies, export conflicts.
```

---

## Project 2 — HTML Page

Run after `final_aws-cloudformation.mp3` is live on R2.

```
Topic: AWS CloudFormation
Slug: aws-cloudformation
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_aws-cloudformation.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
