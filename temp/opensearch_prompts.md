# OpenSearch — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: AWS OpenSearch
Slug: opensearch
Extra coverage required: OpenSearch vs Elasticsearch — history, fork, and compatibility,
index and shard design — how to size shards, over-sharding problems,
mapping and field types — keyword vs text, dynamic vs explicit mapping,
query DSL — match, bool, range, aggregations, nested queries,
relevance scoring and tuning — BM25, boosting, function_score,
OpenSearch Serverless vs managed OpenSearch Service — tradeoffs and cost,
ingestion patterns — Kinesis Data Firehose, Lambda, Logstash, OpenSearch Ingestion,
OpenSearch for observability — logs, traces, and metrics in one store,
data engineering use cases — full-text search over a data lake, log analytics,
anomaly detection plugin for automated alerting on metrics,
ISM policies for index lifecycle management — rollover, retention, deletion,
integration patterns with S3, Kinesis, and the rest of the AWS data stack,
cost model — instance types, storage, data transfer, Serverless OCU pricing.
```

---

## Project 2 — HTML Page

Run after `final_opensearch.mp3` is live on R2.

```
Topic: AWS OpenSearch
Slug: opensearch
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_opensearch.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
