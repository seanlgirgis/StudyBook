# Terraform — ChatGPT Project Prompts

---

## Project 1 — Audio Script

```
Topic: Terraform
Slug: terraform
Extra coverage required: HCL syntax and resource/module/provider structure,
state management (local vs remote S3 backend with DynamoDB locking),
plan/apply/destroy workflow and what each does to state,
modules for reusability across environments,
workspaces vs separate state files for environment promotion,
importing existing AWS resources into state,
Terraform vs CloudFormation — when to choose each,
provisioning real data engineering infrastructure (S3 buckets, IAM roles, VPC, Glue, RDS, ECS),
secrets management — never store secrets in state,
drift detection and remediation,
CI/CD integration — running Terraform in GitHub Actions with OIDC,
common mistakes that corrupt or lose state.
```

---

## Project 2 — HTML Page

Run after `final_terraform.mp3` is live on R2.

```
Topic: Terraform
Slug: terraform
Audio URL: https://pub-174bd65326be4562b4618ccf6a4a8864.r2.dev/final_terraform.mp3
Today's date: 2026-04-25
Generate the complete HTML page.
```
