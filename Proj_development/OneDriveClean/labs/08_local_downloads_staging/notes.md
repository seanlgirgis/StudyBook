# Notes

- Local sources (for example Downloads) are allowed as upstream inputs.
- This lab is still safety-first: no source delete/move/rename.
- Upload to clean OneDrive is out of scope until approval config exists.
