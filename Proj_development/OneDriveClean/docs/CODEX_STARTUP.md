# Codex Startup

- Start in project root.
- Run `..\..\env_setter.ps1`.
- Use relative paths where practical.
- Read config files.
- Never hard-code local operational paths inside Python modules.
- Never delete, rename, move, or sync source data.
- Keep real data outside Git.
- Summarize files changed and tests run at the end.
