# Data Boundary

This Git repo may contain only code/docs/tests/safe templates.

Must not contain real source files, real pod outputs, personal manifests, proposals, local SQLite databases with personal metadata, tokens, or secrets.

All real pod/proposal outputs and databases belong under `D:\AI_Lab\OneDriveClean` and remain outside Git.
