# AGENTS

ODC skills are narrow and task-specific.

Use `ODC_folder_onboarding` only for folder intake into onboarding pods.

Do not use `ODC_folder_onboarding` for:
- vault publishing
- deletion
- source cleanup execution
- broad OneDrive migration

Vault publishing will be a separate skill later.
