# Intake Folder Prompt

Collect folder intake details and prepare a safe copy-first pod onboarding proposal.

Required checks:
- Confirm source path exists.
- Confirm destination pod path is config-derived.
- Produce manifest and metadata stubs without mutating source.