# DATA_REQUIREMENTS.md

- Data model must separate file identity from file location instances.
- Pod-level story/context is required early in lifecycle.
- Sensitivity and storage temperature must be represented.
- Review and publish status must be tracked per file lifecycle.
- Full extracted text is out of scope for v0 DB storage.
- Paths are stored as text and interpreted via machine-local config.