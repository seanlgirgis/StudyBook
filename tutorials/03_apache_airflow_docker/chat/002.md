Exactly — we have **not run anything yet**.

You only saved the file on the host. Now Airflow needs the scheduler/webserver to parse it.

From here, run:

```powershell
cd D:\Workarea\StudyBook\tutorials\03_apache_airflow_docker\docker

docker exec -it docker_airflow-airflow-webserver-1 ls /opt/airflow/dags

docker exec -it docker_airflow-airflow-webserver-1 airflow dags list-import-errors

docker exec -it docker_airflow-airflow-webserver-1 airflow dags list
```

Expected:

```text
studybook_01_dag_basics
```

If `list-import-errors` is empty, wait 10–30 seconds and refresh the UI.

One important note: because your stack commands live under `docker`, switch into:

```powershell
D:\Workarea\StudyBook\tutorials\03_apache_airflow_docker\docker
```

---




Perfect. File 01 is loaded correctly.

You confirmed:

```text
/opt/airflow/dags/studybook_01_dag_basics.py
No import errors
studybook_01_dag_basics listed
```

Now do this in the UI:

1. Toggle `studybook_01_dag_basics` from paused to active.
2. Click the ▶ trigger button.
3. Open the DAG and watch the task chain:

```text
extract → transform → load → check_output_files → notify
```

Key learning checkpoint:

```text
Host file:
docker/dags/studybook_01_dag_basics.py

Container path:
/opt/airflow/dags/studybook_01_dag_basics.py

Runtime output:
inside container at /tmp/studybook/01_dag_basics/
```

When it runs green, say:

```text
generate file 02
```
---

Great — it ran successfully from the GUI.

You now completed the first Docker Airflow learning loop:

```text
host DAG file → mounted into container → parsed by Airflow → triggered in UI → tasks executed
```

The green `5` means all 5 tasks completed:

```text
extract → transform → load → check_output_files → notify
```

You can also verify output inside the container:

```powershell
docker exec -it docker_airflow-airflow-webserver-1 ls -R /tmp/studybook/01_dag_basics
```

Ready for:

```text
generate file 02
```


---

