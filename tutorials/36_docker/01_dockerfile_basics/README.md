# 01 — Dockerfile Basics

Run:

```powershell
python 01_dockerfile_basics.py
```

This lesson builds:
- a large unoptimized image
- a cleaner optimized image

Then it compares sizes, inspects layers, and runs the optimized container with bind-mounted local folders.
