Perfect. Bundle 04 worked exactly right.

What you proved:

```text
docker stop → sends SIGTERM
Python catches SIGTERM
Pipeline finishes current batch
Writes summary file
Container exits cleanly
```

Key proof:

```text
Shutdown signal 15 received
Graceful stop requested after batch 3
Pipeline complete
shutdown_requested: true
```

Small improvement for the script:

```powershell
docker stop --timeout 30 tutorial36-sigterm-demo
```

instead of:

```powershell
docker stop --time 30
```

because Docker says `--time` is deprecated.

You’ve now completed:

```text
01 build/run image
02 multistage builds
03 compose systems
04 production lifecycle behavior
```
