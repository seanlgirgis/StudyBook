Good — let’s do this clean and practical. No guessing.

---

# 🧭 First — quick reality check

That path:

```text
D:\Workarea\StudyBook\tutorials\36_docker\docker_bundle_02_multistage
```

👉 is likely a **folder (after you unzipped)**
OR a `.zip` you downloaded.

---

# ✅ Step 1 — make sure it’s extracted

If you downloaded:

```text
docker_bundle_02_multistage.zip
```

👉 Extract it so you see:

```text
docker_bundle_02_multistage/
  ├── 02_multi_stage_builds.py
  ├── Dockerfile.multistage
  └── Dockerfile.dev
```

---

# ✅ Step 2 — move files to tutorial root

👉 IMPORTANT (this trips people up)

Move these files into:

```text
D:\Workarea\StudyBook\tutorials\36_docker
```

So your folder looks like:

```text
36_docker/
  01_dockerfile_basics.py
  02_multi_stage_builds.py   ✅
  Dockerfile.multistage      ✅
  Dockerfile.dev             ✅
```

---

# ✅ Step 3 — run it

Open PowerShell:

```powershell
cd D:\Workarea\StudyBook\tutorials\36_docker
python 02_multi_stage_builds.py
```

---

# 🧠 What SHOULD happen

You will see:

### 1. Builds happening

```text
docker build -f Dockerfile.multistage ...
```

### 2. Size comparisons printed

Something like:

```text
base:        ~1GB
optimized:   ~500MB
multistage:  ~140MB
```

### 3. Explanations (no container run)

👉 IMPORTANT:
You will NOT see:

* running containers
* `/data/output` files

Because this file is **build + explain only**

---

# 🔍 If nothing happens / errors

Run these checks:

### Check files exist

```powershell
dir
```

You should see:

* `02_multi_stage_builds.py`
* `Dockerfile.multistage`

---

### Check Docker works

```powershell
docker ps
```

---

### Check Python runs file

```powershell
python 02_multi_stage_builds.py
```

---

# ⚠️ Common mistake (very likely)

Running from wrong folder:

❌ This will fail:

```powershell
cd docker_bundle_02_multistage
python 02_multi_stage_builds.py
```

Because:

* It won’t see your `app/` folder
* It won’t see `requirements.txt`

👉 Always run from:

```text
36_docker root
```

---

# 🧠 What 02 is teaching you (in one line)

> “How to build smaller, production-grade Docker images using multi-stage builds.”

---

# 🚀 After you run it

Paste the output here.

I’ll:

* break it down
* show you what improved vs 01
* explain why multistage is a big deal (this is where pros separate)
