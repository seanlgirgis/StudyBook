Perfect. We’ll do **B first**, then upgrade to **A**.

## Bundle 03B — simple Compose demo

Goal:

```text
one command → multiple containers running
```

Services:

```text
web        simple Python app
redis      tiny cache service
```

You’ll learn:

```text
docker compose up
docker compose down
docker compose ps
docker compose logs
docker compose exec
```

Then after that:

## Bundle 03A — real data stack

Services:

```text
pipeline
postgres
redis
```

This matches the full tutorial spec.

---

For now, create/run **03B only**:

```text
D:\Workarea\StudyBook\tutorials\36_docker\03b_compose_simple
```

Suggested files:

```text
03b_compose_simple/
  README.md
  docker-compose.yml
  app/
    app.py
    requirements.txt
    Dockerfile
```

After 03B works, we’ll generate the official **Bundle 03A** in the tutorial root.
