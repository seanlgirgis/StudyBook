This is a **perfect run**. Seriously—this is exactly what we want to see at this stage.

---

# ✅ What just happened (in plain English)

From your run :

### 1. You built TWO images

* **Bad (teaching purpose):**

  ```
  tutorial36-pipeline:base-1.0.0 → 1.88GB
  ```

* **Good (optimized):**

  ```
  tutorial36-pipeline:optimized-1.0.0 → 597MB
  ```

👉 You just learned:

> Same code → 3× smaller image just by writing a better Dockerfile.

---

### 2. You ran a container successfully

```
Processed 1,000 rows -> /data/output/summary.json
```

👉 Translation:

* Docker created a **temporary container**
* Mounted your local folders:

  ```
  host: runtime_data → container: /data
  ```
* Ran your Python pipeline
* Wrote output back to your machine

---

### 3. You inspected layers (THIS is gold)

You saw:

```
618MB → apt-get install (junk)
324MB → pip install
```

vs optimized:

```
312MB → pip install
NO massive apt layer
```

👉 This is the core lesson:

> Bad base image = huge hidden baggage
> Slim base + control = production-ready

---

### 4. You checked Docker state

#### Images:

```
tutorial36-pipeline:base
tutorial36-pipeline:optimized
tutorial36-pipeline:multistage (from earlier)
```

#### Containers:

You have a LOT running (Airflow, Spark, Kafka, etc.)

👉 Important insight:

> Your Docker engine is shared across EVERYTHING
> (not just this tutorial)

---

# ⚠️ One important thing to notice

Your container run:

```
docker run --rm ...
```

That means:

> Container is **created → runs → destroyed automatically**

That’s why:

```powershell
docker ps -a
```

does NOT show your pipeline container.

👉 This is correct behavior.

---

# 🧠 Mental model (lock this in)

```
Image = blueprint
Container = running instance
```

You:

* Built images ✔
* Ran container ✔
* Container exited ✔
* Output persisted via volume ✔

---

# 🔍 One small observation

You still see:

```
tutorial36-pipeline:multistage-1.0.0
```

That came from earlier experiments.

👉 Not wrong, but later we’ll clean naming to:

```
t36_01_*
t36_02_*
```

---

# 🚀 What to do next

You are now ready for **Lesson 02 (multi-stage)**

```powershell
cd ..\02_multi_stage_builds
python 02_multi_stage_builds.py
```

---

# 🧠 Before you go (key takeaway)

What you just did is the **foundation of ALL modern data engineering infra**:

* Build environment → Docker image
* Run workload → container
* Persist data → volume

Everything later (Airflow, Kubernetes, cloud) builds on this.

---

If anything felt unclear, call it out now—this is the layer where misunderstandings become expensive later.
