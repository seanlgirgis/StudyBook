# 03B — Simple Compose Demo

Run:

```powershell
docker compose up --build
```

Open:

```text
http://localhost:8000
```

Stop:

```powershell
docker compose down
```

This runs a Python web container and a Redis container.
