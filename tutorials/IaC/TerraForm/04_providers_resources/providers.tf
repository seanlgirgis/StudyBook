# This lab intentionally uses only local-safe Terraform features.
# No AWS credentials are required.
# The terraform_data resource is built into modern Terraform.
# AWS example is conceptual in tutorial page only.
