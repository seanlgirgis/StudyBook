Create or recreate this exact local-safe lab under:
D:\Workarea\StudyBook\tutorials\IaC\TerraForm\07_environments_workspaces

Guardrails:
- do not invent cloud credentials
- do not use AWS
- do not run terraform apply
- do not create cloud resources
- do not configure a real remote backend
- create the exact files requested
- use local-safe terraform_data only
- run `terraform version`, `terraform init`, `terraform fmt`, `terraform validate`, and `terraform plan` only if Terraform is installed
- report results clearly
